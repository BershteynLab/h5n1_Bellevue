# Website-only repo mode
# Ignore everything by default, then allow only website files.
*

# Keep gitignore itself and optional top-level docs/domain files
!.gitignore
!README.md
!CNAME

# Publish site content
!risk_map/
!risk_map/**

# Keep archive backups local-only
archive/
